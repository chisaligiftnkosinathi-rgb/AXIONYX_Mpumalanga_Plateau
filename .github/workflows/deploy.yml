name: Deploy Pipeline
on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deployment Placeholder
        run: echo "Manual deployment triggered. Pending VPS configuration."
