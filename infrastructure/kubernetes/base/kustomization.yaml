apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# External Secrets Operator configuration is merged here
# ensuring credentials map securely from Google Secret Manager to the Pods.
